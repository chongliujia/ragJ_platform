## Screenshots

This folder contains screenshots referenced by the root `README.md`.

- Keep filenames stable to avoid updating README links.
- Prefer `png` or `webp` for screenshots.
- Avoid including sensitive information (API keys, internal URLs, customer data) before making the repo public.

